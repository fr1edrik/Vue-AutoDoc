// import axios from 'axios'

export default class Util {
	// print(file: string) {
	// 	axios.get('http://localhost:8081/files').then(e => console.log(e))
	// }
	// static parse(string: string) {
	// 	const reg = /^<(template).(\s.*)*<\/(template)>$/m
	// 	let regResult = reg.exec(string)?reg.exec(string)[0]:'';
	// 	const template = regResult
	// 	return template
	// }
}
